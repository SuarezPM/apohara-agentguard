# Rust build output
/target

# Cargo.lock is intentionally committed (this is a binary project).

# cargo-fuzz (fuzz/ is its own separate crate; build + run artifacts)
/fuzz/target
/fuzz/corpus
/fuzz/artifacts
/fuzz/Cargo.lock

# Editors / OS
/.idea
/.vscode
*.swp
.DS_Store

# Local assistant working files (not part of the project)
/.claude
/.omc
/.cocoindex_code
/AGENTS.md
/CLAUDE.md
/CLAUDE.local.md

# Deepwork session state (git-local, OpenCode-readable)
.slim/deepwork/
.slim/

# Working analysis docs (local-only; not part of the published repo)
/docs/

# Generated/local tooling artifacts
codemap.md
/HANDOFF.md
